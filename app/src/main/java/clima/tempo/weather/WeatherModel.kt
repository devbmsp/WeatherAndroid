package clima.tempo.weather

data class WeatherModel(var x : Int)
// Funções utilizadas e dados da api,bd

class WeatherRepository{
    //private var _info = WeatherModel(0)
   // fun getInformation() = _info
}